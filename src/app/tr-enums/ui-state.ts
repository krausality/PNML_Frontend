export enum TabState {
    Build,
    Play,
    Save,
    Code,
}

export enum ButtonState {
    Blitz,
    Place,
    Transition,
    Arc,
    Anchor,
    Add,
    Remove,
    Delete,
    Clear,
    Move,
    Select,
}

export enum CodeEditorFormat {
    JSON,
    PNML,
}