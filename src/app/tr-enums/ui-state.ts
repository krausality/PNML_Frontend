export enum TabState {
    Build,
    Play,
    Save,
}

export enum ButtonState {
    Blitz,
    Place,
    Transition,
    Arc,
    Anchor,
    Add,
    Remove,
    Delete,
    Clear,
    Move,
    Select,
}
