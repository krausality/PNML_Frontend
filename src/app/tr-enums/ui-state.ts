export enum TabState {
    Build,
    Play,
    Save,
    Analyze,
}

export enum ButtonState {
    Blitz,
    Place,
    Transition,
    Arc,
    Anchor,
    Add,
    Remove,
    Delete,
    Clear,
    Move,
    Select,
}
