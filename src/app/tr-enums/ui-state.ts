export enum TabState {
    Build,
    Play,
    Save,
    Analyze,
    Code,
    Analyze,
}

export enum ButtonState {
    Blitz,
    Place,
    Transition,
    Arc,
    Anchor,
    Add,
    Remove,
    Delete,
    Clear,
    Move,
    Select,
    ApplyCode,
}

export enum CodeEditorFormat {
    JSON,
    PNML,
}
